{{- $secret := .Values.admin.existingSecret | default (printf "%s-admin" .Release.Name) }}
Admin console password:
  kubectl get secret {{ $secret }} -n {{ .Release.Namespace }} -o jsonpath='{.data.password}' | base64 -d

Port-forward to the console:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ .Release.Name }} {{ .Values.service.port }}:{{ .Values.service.port }}
