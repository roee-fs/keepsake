# Python tooling for e2e/ and the chart tests only. The server is Go.
[tool.uv]
package = false

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruamel-yaml>=0.19.1",
    "ruff>=0.16.7",
    "ty>=0.0.81",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
